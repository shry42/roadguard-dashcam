PORT=8080
# Required for internet / HTTPS tunnel — public WebSocket URL the phone app uses:
PUBLIC_WS_URL=wss://stream.yourdomain.com
# Optional: your own TURN server (otherwise free Metered Open Relay is used)
# TURN_URL=turn:your-turn.example.com:3478
# TURN_USER=user
# TURN_CRED=secret
